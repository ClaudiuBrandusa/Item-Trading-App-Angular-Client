import { RefreshTokenRequest } from './refresh-token-request.model';

describe('RefreshTokenRequest', () => {
  it('should create an instance', () => {
    expect(new RefreshTokenRequest()).toBeTruthy();
  });
});
